export interface NCFConfig {
  rangeStart: string;
  rangeEnd: string;
  lastAssigned?: string | null;
}

export interface CFConfig {
  rangeStart: string;
  rangeEnd: string;
  lastAssigned?: string | null;
}